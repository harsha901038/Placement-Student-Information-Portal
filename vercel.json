{
  "buildCommand": "npm install vite --save-dev && ./node_modules/.bin/vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}